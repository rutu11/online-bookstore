spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orderservice_db
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: order-service

server:
  servlet:
    context-path: /api
  port: 8081